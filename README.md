To solve the route destination through a route, you only need to register RouterController in the Any function, and then instantiate the corresponding method to implement the ViewsBehavior interface.

